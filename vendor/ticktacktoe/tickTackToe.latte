{block content}

<h1>Tick Tack Toe</h1>

<a n:href="newGame!" class="ajax">new game</a>

<div id="playboard">

	{snippet board}
		{foreach $board as $row}

			<div class="row">

				{foreach $row as $square}
					{if $square->isBlank()}
						<a n:href="makeMove! $square->getX(), $square->getY()" class="ajax square"></a>
					{else}
						<div class="square {$square->getShape()}"></div>
					{/if}
				{/foreach}

			</div>

		{/foreach}
	{/snippet}

</div>